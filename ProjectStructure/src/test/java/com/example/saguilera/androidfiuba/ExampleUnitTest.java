package com.example.saguilera.androidfiuba;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Here you should have your unit tests. As unit testing in android requires of external frameworks
 * and its not something too easy to do.
 * We will skip android testing (plus its not required for the subject)
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() throws Exception {
        assertEquals(4, 2 + 2);
    }
}